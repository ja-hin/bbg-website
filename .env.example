# PayU Configuration - Currently using test credentials
# Test credentials are hardcoded in the application for development
# For production, uncomment and set your actual PayU merchant credentials:
# PAYU_MERCHANT_KEY=your_production_merchant_key
# PAYU_SALT=your_production_salt_key  
# PAYU_BASE_URL=https://secure.payu.in

# Database Configuration (Already configured for SQL Server)
# SQL Server connection is configured in server/db.ts

# Development Configuration
NODE_ENV=development