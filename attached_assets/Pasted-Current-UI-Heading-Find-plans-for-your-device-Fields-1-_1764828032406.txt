Current UI:
- Heading: Find plans for your device
- Fields:
  1. Device Type (select)
  2. Device Purchase Date (date input)
  3. Find Plans button

I want you to add a new field for Device Brand and fetch the options from our existing backend.

Requirements:

1) New field placement
- Add a new dropdown labeled Device Brand.
- Position: directly below Device Type and above Device Purchase Date.
- Use the same JSX structure and Tailwind classes as the Device Type dropdown so the styling matches.

2) Fetching brands from backend
- Do not hardcode any brand list.
- Inspect the codebase and reuse the existing API or service that already returns brands from our backend database.
  - For example, if there is anything like:
    - an API client for brand master
    - functions such as getBrands, fetchBrands, getBrandList, etc.
  - Use the same pattern that other parts of the app use to call this API.
- If the backend supports filtering by device type, then:
  - When a Device Type is selected, call the API with that type and update the Device Brand options.
  - Before a type is selected, Device Brand should be disabled.
- If the backend does not filter by type, just fetch the full brand list once on component mount.

3) Form behavior
- Integrate Device Brand into the existing form state and submit handler.
- Treat it as a required field in the same way Device Type is handled.
- When the user clicks Find Plans, the payload should now include:
  - deviceType
  - deviceBrand
  - devicePurchaseDate
- Handle loading state for the brand dropdown if needed:
  - Disabled while loading
  - Simple fallback text like “Loading brands…” in the dropdown.

4) UI details
- Label text: Device Brand
- Placeholder: Select device brand
- Same error and helper text style used elsewhere in this form.
- Keep all existing fields and layout intact.

Output:
- Provide the updated React component (or the relevant JSX + hook code) showing:
  - Device Type select
  - Device Brand select with fetched options
  - Device Purchase Date field
  - Find Plans button