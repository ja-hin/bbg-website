We are using React + Tailwind for this “Find plans for your device” form.

Problem:
In the 3 fields (Device Type, Device Brand, Device Purchase Date) the visual styling is not consistent:
- The text / placeholder color for Device Brand looks more muted than the other two.
- The dropdown arrows for Device Type and Device Brand are not visually identical.

Goal:
Make all 3 fields look visually consistent while keeping the existing functionality.

Instructions:

1) Label styling
- Ensure the labels “Device Type”, “Device Brand”, and “Device Purchase Date” all use the same Tailwind text classes.
- Use the same font size, weight, and color (for example `text-sm font-medium text-gray-700` or whatever is already applied to Device Type).

2) Input / select styling
- For all three controls (both selects and the date input), ensure:
  - Same background color
  - Same border color and radius
  - Same padding and font size
  - Same focus styles (ring color, border color)
- Use a shared set of Tailwind classes. You can extract a common class string like `formFieldBase` or just copy the Device Type select styling to the other two.

3) Placeholder / text color
- Make the placeholder text color consistent across all 3 fields.
- Device Brand currently looks muted: remove any extra `text-gray-400` or different opacity only on that field.
- Use the same placeholder classes as Device Type (for example `placeholder:text-gray-400`).

4) Dropdown arrows
- Make the dropdown arrows for Device Type and Device Brand identical:
  - Either use the same native select arrow (no custom icon) for both, OR
  - Use the same custom chevron icon for both (for example a Heroicons `ChevronDownIcon`) with identical size and positioning.
- If using a custom icon, ensure both selects use:
  - `appearance-none`
  - Right padding to accommodate the icon
  - A shared wrapper `relative` container and the same absolute positioning for the chevron.

5) Do not change:
- Field order
- Field names
- Any submit logic or API integration

Output:
Return the updated JSX for this form section (Device Type, Device Brand, Device Purchase Date, Find Plans) with unified Tailwind classes and consistent dropdown arrows.
