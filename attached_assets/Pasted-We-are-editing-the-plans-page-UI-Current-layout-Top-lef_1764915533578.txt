We are editing the /plans page UI.

Current layout:
- Top left: “Back to Home” link
- Center: heading “Available Plans for Your Device”
- Below heading: chips for Type, Brand, Purchase Date
- Then a lot of empty vertical space
- Then the plan cards grid

Goals:
1) Reduce vertical space:
   a) Between “Back to Home” and the main heading.
   b) Between the heading / chips block and the content below.
2) Insert a small benefit strip between the heading block and the plan cards:
   - Use ONLY these four graphics:
     • No Hidden Charges
     • Free Doorstep Pickup
     • Instant Payouts
     • Completely Digital
   - Do NOT use the “Best BuyBack Value” graphic.
3) Do not change the design of the plan cards themselves or any business logic.

Implementation details:

1) Spacing adjustments
- Find the container that wraps:
  - Back to Home
  - The heading “Available Plans for Your Device”
  - The chips row
- Reduce margin / padding utilities (mt-*, mb-*, pt-*, pb-*) so:
  - The gap between Back to Home and the heading is small and tidy.
  - The gap below the chips is also tighter than it is now.
- Also check the top margin on the section that contains the cards and lower it if needed.

2) Benefit graphics row
- Between the chips row and the plan cards, add a new row of four benefit items.
- Each benefit item should show:
  - Icon on top
  - Label text under it
- Use only four benefits:
  - No Hidden Charges
  - Free Doorstep Pickup
  - Instant Payouts
  - Completely Digital
- Do NOT render the Best BuyBack Value icon.
- Layout:
  - On desktop: show them in a single horizontal row using flex (justify-center, gap-x-8 or similar).
  - On mobile: allow wrapping into 2x2 or 4 stacked items using flex-wrap.
- Center-align the icons and text.
- Keep icon size consistent and visually balanced with the rest of the page.
- Use Tailwind classes only, consistent with existing design.

Example structure (you can adapt to match current code):

<div className="mt-4 mb-6 flex flex-wrap items-center justify-center gap-6">
  {/* Benefit 1: No Hidden Charges */}
  {/* Benefit 2: Free Doorstep Pickup */}
  {/* Benefit 3: Instant Payouts */}
  {/* Benefit 4: Completely Digital */}
</div>

Hook up the icons using the existing asset paths (for example: /assets/no-hidden-charges.svg, etc.) or whatever naming is already in the project.

3) Keep everything else as is
- Do not touch:
  - Card typography, colors, or sizes
  - Plan visibility conditions
  - Back to Home behavior

Output:
- Updated JSX for the /plans page section showing:
  - Reduced spacing above the heading
  - Reduced spacing below the chips
  - New benefit icons row with only the four specified graphics
  - Plan cards appearing directly below this new row.
