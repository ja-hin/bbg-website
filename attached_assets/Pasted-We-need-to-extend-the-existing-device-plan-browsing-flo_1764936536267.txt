We need to extend the existing device-plan browsing flow.

Current situation:
- On the home page, the “Find plans for your device” form (screenshot 1) is displayed inline.
- When submitted, it navigates to /plans and uses the existing logic to show the relevant plans based on device type + brand + purchase date.

New requirement:
- The top navigation has a “Buy” menu item (screenshot 2).
- When the customer clicks “Buy”, it should open a popup modal containing the *exact same* form UI/UX and fields used on the homepage (screenshot 1).
- The modal must be closeable.
- Once the user fills the form and clicks “View Plans”, the *existing* View Plans flow should run exactly the same as today: redirect to /plans and show only the applicable plans.
- No new logic or new form should be created. Reuse all existing logic (form state, validations, brand fetching, date logic, submit logic, and navigation to /plans).

Implementation instructions:

1. Create a reusable component:
   - Extract the existing “Find plans for your device” form into a separate component, for example:
     `DevicePlanSelectorForm.jsx`
   - Ensure this component contains:
     - Device Type dropdown (with dynamic brand fetch)
     - Device Brand dropdown
     - Device Purchase Date
     - Submit handler that navigates to /plans with the same state payload as today

2. Replace the inline form with the extracted component:
   - On the homepage, render <DevicePlanSelectorForm /> exactly where the form currently is.
   - Confirm all functionality remains unchanged.

3. Add a modal:
   - Create a modal using the project’s existing modal pattern (if already exists). Else create a Tailwind-based modal.
   - Modal should have:
     - White background, rounded corners, drop shadow similar to screenshot 1
     - A close “X” icon on the top-right
     - Centered full-screen overlay
     - Scroll-lock when open

4. Connecting the nav:
   - In the navigation bar, attach an onClick handler to the “Buy” menu item:
     - Opening the modal
     - Rendering <DevicePlanSelectorForm /> **inside the modal**

5. Submit behavior inside the modal:
   - When the user submits the form from inside the popup:
     - The modal should close automatically
     - The existing submit handler should run as usual:
       → navigate('/plans', { state: { deviceType, deviceBrand, devicePurchaseDate } })
       → /plans will display applicable plans (BBG + Extend+ or only Extend+)

6. No redesign:
   - The form in the modal should look identical to screenshot 1
   - Same spacing, typography, fields, placeholder styling, etc
   - Just wrapped inside a centered popup

7. Output required:
   - Updated code for:
     a) Extracted `DevicePlanSelectorForm` component
     b) Modal component with close button
     c) Navigation bar handler that opens the modal
     d) Updated homepage form usage
   - Do NOT change any logic of the plan selection or plan rendering.
   - The entire “View Plans” journey must remain exactly as implemented.

